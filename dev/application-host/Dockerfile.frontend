FROM nginx:alpine

WORKDIR /executables
COPY frontend.tar.gz .
RUN tar -xf frontend.tar.gz -C /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/nginx.conf
COPY cert.pem /etc/nginx/cert.pem
COPY privkey.pem /etc/nginx/privkey.pem